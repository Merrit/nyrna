/// Run-time environmental variables.
class Config {
  /// If toggle is true => toggle suspend for active window,
  /// do not load GUI.
  static bool toggle = false;

  /// If true, enable logging functionality.
  static bool log = false;
}
